---
import { emphasize } from '#lib/text';
import type { SiteContent } from '#i18n/types';

interface Props {
  content: SiteContent;
}
const { features } = Astro.props.content;
---

<section id="features" class="mx-auto max-w-6xl px-5 py-14 sm:py-20 lg:py-24">
  <div class="reveal mx-auto max-w-2xl text-center">
    <p class="mb-3 font-mono text-xs tracking-widest text-teal uppercase">{features.label}</p>
    <h2 class="text-3xl font-bold tracking-tight text-balance sm:text-4xl">{features.title}</h2>
  </div>

  {/* Flex + wrap + justify-center: cards stay a uniform size, and an incomplete
      last row is centered rather than left-aligned with a stray gap. */}
  <div class="mt-14 flex flex-wrap justify-center gap-4">
    {
      features.items.map((item, i) => (
        <article
          class="reveal group relative w-full overflow-hidden rounded-2xl border border-line bg-panel/30 p-6 transition-all duration-300 hover:-translate-y-1 hover:border-line-2 hover:bg-panel/60 md:w-[calc(50%-0.5rem)] lg:w-[calc(33.333%-0.667rem)]"
          style={`--reveal-delay: ${(i % 3) * 80}ms`}
        >
          <div class="mb-4 inline-flex h-9 w-9 items-center justify-center rounded-lg border border-line-2 bg-bg-2 font-mono text-sm text-teal">
            {String(i + 1).padStart(2, '0')}
          </div>
          <h3 class="font-semibold tracking-tight">{item.title}</h3>
          <p class="mt-2 text-sm leading-relaxed text-ink-dim" set:html={emphasize(item.desc)} />
        </article>
      ))
    }
  </div>
</section>

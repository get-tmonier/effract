# Exact versions only — the catalog is the single source of truth.
save-exact=true
# tsgo / @typescript/native-preview is the only TypeScript checker; some deps
# declare `typescript` as an optional peer. Don't let pnpm warn about it.
strict-peer-dependencies=false
auto-install-peers=true
